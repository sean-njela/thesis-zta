{
  "name": "Cloud Devbox + mise",
  "image": "ghcr.io/sean-njela/dev-env:latest",
  "postCreateCommand": "devbox install && mise install",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
